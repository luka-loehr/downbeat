{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "downbeat",
  "main": "src/worker/index.ts",
  "compatibility_date": "2026-08-01",
  "compatibility_flags": ["nodejs_compat"],
  "assets": {
    "directory": "./dist/client",
    "binding": "ASSETS",
    "not_found_handling": "single-page-application"
  },
  "durable_objects": {
    "bindings": [{ "name": "ROOM", "class_name": "RoomDO" }]
  },
  // v2/v3 are the scar tissue of the cloud-source experiment (see the
  // cloud-source branch): the namespace briefly carried a SourceContainer
  // class, and deleting a DO class needs an explicit migration.
  "migrations": [
    { "tag": "v1", "new_sqlite_classes": ["RoomDO"] },
    { "tag": "v2", "new_sqlite_classes": ["SourceContainer"] },
    { "tag": "v3", "deleted_classes": ["SourceContainer"] }
  ],
  "r2_buckets": [{ "binding": "AUDIO", "bucket_name": "downbeat-audio" }],
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "downbeat-sessions",
      "database_id": "00000000-0000-0000-0000-000000000000",
      "migrations_dir": "migrations"
    }
  ],
  "triggers": { "crons": ["17 * * * *"] },
  "routes": [{ "pattern": "downbeat.lukaloehr.com", "custom_domain": true }],
  "workers_dev": true,
  "preview_urls": false,
  "observability": { "enabled": true },
  "vars": { "ENVIRONMENT": "production" }
}
